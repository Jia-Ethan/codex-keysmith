# codex-keysmith v0.3.9

<!-- WINDOWS_FRESH_DEPLOYMENT_POLICY: EXPLICIT_BETA -->

这是尚未发布的 `v0.3.9` 候选说明。

新增受约束的 `--reactivate`：仅在 manifest 与受管 Markdown 健康、顶层 `model_instructions_file` 缺失时，备份当前配置并补回该字段。多目录 reactivate 若后续目录失败，会回滚本次调用已写入的全部目录，避免非零退出后留下部分提交。
